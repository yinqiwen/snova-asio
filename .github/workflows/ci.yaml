on:
  push:
    branches:
      - main
  pull_request:
    types: [ assigned, opened, synchronize, reopened, labeled ]
jobs:
  linux_build:
    runs-on: ubuntu-latest
    steps:
      - uses: bazelbuild/setup-bazelisk@v2
      - run: cd $GITHUB_WORKSPACE; bazel build --cxxopt=-std=c++20  ...
